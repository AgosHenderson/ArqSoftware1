package services

import "github.com/AgosHenderson/ArqSoftware1/ej-auth\domain"

func Login(cred domain.Credentials) (domain.Token, error) {
	utils.ReadFile("")
	// read credentials
	// validate credentials

	utils.ReadFile("")
	// read token
	return domain.Token{}, nil
}
